
  # Medical Web App

  This is a code bundle for Medical Web App.
  This is developed for the School of ISI, Kyushu University.
  
  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  
